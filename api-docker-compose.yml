version: '3.3'
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    command: nodemon main.js
    volumes:
      - .:/usr/app/
      - /usr/app/node_modules
    ports:
      - "3333:3333"
