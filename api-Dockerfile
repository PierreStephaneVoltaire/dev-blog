FROM node:12.13-alpine

ARG NODE_ENV=production
ENV NODE_ENV=${NODE_ENV}

WORKDIR /usr/app

COPY package*.json ./
RUN sudo apt update
RUN sudo apt install yarn
RUN yarn
RUN yarn build api


COPY  dist/apps/api /usr/app/
